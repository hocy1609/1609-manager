diff --git a/README.md b/README.md
new file mode 100644
index 0000000000000000000000000000000000000000..ae2ec7571ca78c231a561820cfd6a6f7d9b0a47f
--- /dev/null
+++ b/README.md
@@ -0,0 +1,46 @@
+# 1609 Manager
+
+16:09 Launcher — менеджер для **Neverwinter Nights: Enhanced Edition**. Помогает запускать игру с профилями, автоматизировать крафт и следить за логами, чтобы упростить ежедневные игровые рутины.
+
+## ✨ Возможности
+- **Профили и быстрый запуск**: хранение аккаунтов и быстрый старт клиента.
+- **Крафт-автоматизация**: запись drag-макросов, массовое создание и управление зельями.
+- **Горячие клавиши**: отправка последовательностей клавиш прямо в окно NWN.
+- **Мониторинг логов**: отслеживание событий и отправка уведомлений через webhooks.
+- **Настройки и темы**: удобная конфигурация приложения и внешнего вида.
+
+## 🧰 Требования
+- **Windows** (используются Windows API и пути по умолчанию).
+- **Python 3.11+**.
+
+Зависимости устанавливаются из `requirements.txt`:
+
+```bash
+pip install -r requirements.txt
+```
+
+## 🚀 Запуск
+```bash
+python app.py
+```
+
+Все пользовательские данные и логи сохраняются в папке **`1609 settings`** рядом с исполняемым файлом/проектом.
+
+## 📦 Сборка (Windows)
+Для сборки standalone `.exe` используется **PyInstaller** и скрипт `build.bat`.
+
+```bat
+build.bat
+```
+
+Готовый файл появится в `dist/1609Manager.exe`.
+
+## 🧭 Структура проекта
+- `app.py` — точка входа, инициализация приложения.
+- `core/` — бизнес-логика (профили, серверы, лог-монитор, хоткеи, темы).
+- `ui/` — экраны и диалоги интерфейса.
+- `utils/` — вспомогательные утилиты (автофокус, лог-мониторинг, win automation).
+- `assets/`, `fonts/` — ресурсы и шрифты.
+
+## 🤝 Вклад
+PR и предложения приветствуются. Если нашли проблему — создайте issue и опишите шаги воспроизведения.

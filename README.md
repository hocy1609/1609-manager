16:09 Launcher — менеджер для **Neverwinter Nights: Enhanced Edition**. Помогает запускать игру с профилями, управлять серверами и следить за логами, чтобы упростить ежедневные игровые рутины.

## ✨ Возможности
- **Профили и быстрый запуск**: хранение аккаунтов и моментальный старт клиента с нужными параметрами.
  - *Групповой запуск:* выделяйте несколько профилей через `Ctrl`/`Shift` и запускайте их одной кнопкой.
  - *Запуск по категориям:* запускайте все профили одной категории нажатием кнопки ▶️ рядом с её названием.
  - *Сортировка категорий:* настраивайте порядок вывода категорий в списке (⬆/⬇).
- **Управление серверами**: группировка серверов, автоматическое подключение и удобный выбор.
- **Менеджер CD-ключей**: хранение списка ключей и быстрое переключение между ними.
- **Горячие клавиши**: отправка последовательностей клавиш прямо в активное окно NWN. Хоткеи срабатывают умным образом — только если они включены для конкретного профиля, окно которого сейчас активно.
- **Мониторинг логов**: отслеживание событий в реальном времени и отправка уведомлений через Discord Webhooks.
- **Автоматизация настроек**: автоматическая правка `settings.tml` (разрешение, масштаб UI, полноэкранный режим) для комфортной игры.
- **Умные бэкапы**: автоматическое резервирование и гибкое восстановление данных с поддержкой слияния профилей.
- **Настройки и темы**: кастомизация интерфейса (6 цветовых схем), сворачивание в трей и автозагрузка.

## 🧰 Требования
- **Windows** (используются Windows API и пути по умолчанию).
- **Python 3.11+**.

Зависимости устанавливаются из `requirements.txt`:

```bash
pip install -r requirements.txt
```

## 🚀 Запуск
```bash
python app.py
```

Все пользовательские данные и логи сохраняются в папке **`1609 settings`** рядом с исполняемым файлом/проектом.

## 📦 Сборка (Windows)
Для сборки standalone `.exe` используется **PyInstaller** и скрипт `build.bat`.

```bat
build.bat
```

Готовый файл появится в `dist/1609Manager.exe`.

## 🧭 Структура проекта
- `app.py` — точка входа, инициализация приложения.
- `core/` — бизнес-логика (профили, серверы, лог-монитор, хоткеи, темы, бэкапы).
- `ui/` — экраны и диалоги интерфейса.
- `utils/` — вспомогательные утилиты (автофокус, лог-мониторинг, win automation).
- `assets/`, `fonts/` — ресурсы и шрифты.

## 🤝 Вклад
PR и предложения приветствуются. Если нашли проблему — создайте issue и опишите шаги воспроизведения.
